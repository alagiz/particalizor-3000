import styled from "styled-components";

const RefCanvas = styled.div`
  display: none;
`;

const DestinationCanvas = styled.div`
  display: flex;
`;

export const Styled = {
  RefCanvas,
  DestinationCanvas
};
